# S4b-3 演练预案（依冻结契约预写）

> 工作层预案。`s4b3-waiver-drill.sh` 按 #40 冻结契约预写，S4b-3 合入后一键执行。覆盖：policy GET/PUT（削弱 overlay 拒绝）、gates/evidence GET、waiver 合法请求、>7 天负例、**distinct-approver 自批负例**、401 负例。OIDC 面补充用例（waiver.request 权限 403）待 Keycloak 栈就绪后追加。
